/**
 * The Latin web-safe fonts.
 * A screen serving documents in a particular language hands in its own list
 * (`presets.fonts` on `DocxEditor`).
 */
export const DEFAULT_FONTS: readonly string[] = [
  "Arial",
  "Times New Roman",
  "Georgia",
  "Trebuchet MS",
  "Courier New",
];
