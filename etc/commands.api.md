## API Report File for "@portone/docx-editor"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Command } from 'prosemirror-state';
import { EditorState } from 'prosemirror-state';
import type { EditorView } from 'prosemirror-view';
import type { Node as Node_2 } from 'prosemirror-model';

// @public
export type ActiveFontFamily =
/** Every selected character has the same font. A font given by a paragraph style lands here too */
    {
    kind: "font";
    name: string;
}
/** Nobody wrote down a font, so `name` is the font that actually gets rendered */
| {
    kind: "default";
    name: string;
}
/** There are several fonts */
| {
    kind: "mixed";
};

// @public (undocumented)
export function activeFontFamily(state: EditorState, fontFallbacks?: FontFallbacks): ActiveFontFamily;

// @public
export type ActiveFontSize =
/** Every selected character has the same size. A size given by a paragraph style lands here too */
    {
    kind: "size";
    pt: number;
}
/** Nobody wrote down a size, so `pt` is the size that actually gets rendered */
| {
    kind: "default";
    pt: number;
}
/** There are several sizes */
| {
    kind: "mixed";
};

// @public (undocumented)
export function activeFontSize(state: EditorState): ActiveFontSize;

// @public
export function activeLineSpacing(state: EditorState): LineSpacing | null;

// @public
export function activeLink(state: EditorState): string | null;

// @public
export interface ActiveLinkSpan {
    // (undocumented)
    from: number;
    href: string | null;
    // (undocumented)
    to: number;
}

// @public
export function activeLinkSpan(state: EditorState): ActiveLinkSpan | null;

// @public
export function activeListKind(state: EditorState): ListKind | null;

// @public
export type ActiveParagraphAlign =
/** They are all drawn with the same one */
    {
    kind: "shared";
    align: ParagraphAlign;
}
/** They are drawn with several different ones, or the selection holds no paragraph at all */
| {
    kind: "mixed";
};

// @public (undocumented)
export function activeParagraphAlign(state: EditorState): ActiveParagraphAlign;

// @public
export type ActiveParagraphStyle =
/** They all point at the same one. A null id is a paragraph wearing the default style */
    {
    kind: "shared";
    styleId: string | null;
}
/** They point at several different ones */
| {
    kind: "mixed";
}
/** The selection holds no paragraph at all, so there is no style to show or to change */
| {
    kind: "none";
};

// @public (undocumented)
export function activeParagraphStyle(state: EditorState): ActiveParagraphStyle;

// @public (undocumented)
export function activeTextBackground(state: EditorState): string | null;

// @public (undocumented)
export function activeTextColor(state: EditorState): string | null;

// @public
export function addComment(comment: NewComment, at?: CommentRange): Command;

// @public
export function addCommentReply(id: string, reply: NewComment): Command;

// @public (undocumented)
const ALIGNS: readonly ["left", "center", "right", "justify"];

// @public
export function canAddComment(state: EditorState, at?: CommentRange): boolean;

// @public
export function canDecreaseIndent(state: EditorState): boolean;

// @public
export function canEditComment(state: EditorState, commentId: string, replyId?: string | null): boolean;

// @public
export function canExport(state: EditorState): boolean;

// @public
export function canFormatText(state: EditorState): boolean;

// @public
export function canIncreaseIndent(state: EditorState): boolean;

// @public
export function canInsertFootnote(state: EditorState): boolean;

// @public
export function canInsertImage(state: EditorState): boolean;

// @public
export function canInsertTable(state: EditorState): boolean;

// @public (undocumented)
export function canRunCommand(command: Command, state: EditorState): boolean;

// @public
export function canSetLineSpacing(state: EditorState): boolean;

// @public
export function canSetLink(state: EditorState): boolean;

// @public
export function canSetParagraphAlign(state: EditorState): boolean;

// @public
export interface CommentAuthor {
    id: string;
    // (undocumented)
    initials?: string;
    // (undocumented)
    name: string;
}

// @public
export interface CommentRange {
    // (undocumented)
    from: number;
    // (undocumented)
    to: number;
}

// @public (undocumented)
export const decreaseIndent: Command;

// @public
export const decreaseListLevel: Command;

// @public
export function documentBodyWidthPx(state: EditorState): number;

// @public
export interface DocumentComment {
    readonly anchored: boolean;
    // (undocumented)
    readonly author: string | null;
    // (undocumented)
    readonly authorId: string | null;
    // (undocumented)
    readonly date: string | null;
    // (undocumented)
    readonly from: number;
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly initials: string | null;
    // (undocumented)
    readonly referencePos: number;
    // (undocumented)
    readonly replies: readonly DocumentCommentReply[];
    // (undocumented)
    readonly resolved: boolean;
    readonly text: string;
    // (undocumented)
    readonly to: number;
}

// @public (undocumented)
export interface DocumentCommentReply {
    // (undocumented)
    readonly author: string | null;
    // (undocumented)
    readonly authorId: string | null;
    // (undocumented)
    readonly date: string | null;
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly initials: string | null;
    // (undocumented)
    readonly text: string;
}

// @public
export function documentComments(state: EditorState): readonly DocumentComment[];

// @public
interface DocumentDefaults {
    // (undocumented)
    fontFamily: string | null;
    // (undocumented)
    fontSizePt: number | null;
    // (undocumented)
    lineSpacing: LineSpacing | null;
}

// Warning: (ae-forgotten-export) The symbol "DocumentDefaults" needs to be exported by the entry point index.d.ts
//
// @public
export function documentDefaults(state: EditorState): DocumentDefaults;

// @public
export function documentExportProblems(state: EditorState): readonly ExportProblem[];

// @public
export function documentFidelity(state: EditorState): readonly FidelityNote[];

// @public
export function documentFontNames(doc: Node_2, defaults: DocumentDefaults): string[];

// @public
export function documentHasLocked(doc: Node_2): boolean;

// @public (undocumented)
export interface DocumentNote {
    // (undocumented)
    readonly id: string;
    // Warning: (ae-forgotten-export) The symbol "NoteKind" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly kind: NoteKind;
    // (undocumented)
    readonly label: string;
    // (undocumented)
    readonly referencePos: number;
    // (undocumented)
    readonly text: string;
}

// @public
export function documentNotes(state: EditorState): readonly DocumentNote[];

// @public
export function documentParagraphStyles(state: EditorState): ParagraphStyleOption[];

// @public
export type DocxExportErrorCode = "missing-content-types" | "unsupported-content" | "lost-original" | "malformed-xml" | "invalid-table";

// @public
export type EditableComments = "own" | "all";

// @public (undocumented)
export type EditingProtection = "none" | "readOnly" | "comments";

// @public (undocumented)
export function editingProtection(state: EditorState): EditingProtection;

// @public (undocumented)
export type EditorCommand = Command;

// @public
export interface ExportProblem {
    // (undocumented)
    readonly code: DocxExportErrorCode;
    // (undocumented)
    readonly message: string;
    readonly pos?: number;
}

// @public (undocumented)
export type FidelityCode = "preserved-run-content" | "preserved-inline" | "preserved-block" | "range-marker" | "paragraph-demoted" | "table-demoted";

// @public (undocumented)
export interface FidelityNote {
    block: number | null;
    // (undocumented)
    code: FidelityCode;
    element: string;
    part: string | null;
    pos: number | null;
    // (undocumented)
    severity: FidelitySeverity;
}

// @public (undocumented)
export type FidelitySeverity =
/** Carried through whole, with nothing of it on screen */
"hidden"
/** Carried through whole behind a placeholder that stands in its place and cannot be edited */
| "placeholder"
/** Written back as something near what arrived rather than as what arrived */
| "approximated";

// @public
interface FileCarrier {
    // (undocumented)
    readonly files: ArrayLike<File> | null;
}

// @public
export function fittedExtent(width: number, height: number, maxWidthPx?: number, maxHeightPx?: number): ImageExtent | null;

// @public
interface FontFallbackGroup {
    readonly names: readonly string[];
    readonly stack: string;
}

// @public
export interface FontFallbacks {
    readonly defaultFontName: string;
    readonly defaultStack: string;
    // Warning: (ae-forgotten-export) The symbol "FontFallbackGroup" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly groups: readonly FontFallbackGroup[];
}

// @public
export const IMAGE_FILE_ACCEPT: string;

// @public
export interface ImageExtent {
    // (undocumented)
    cx: number;
    // (undocumented)
    cy: number;
}

// Warning: (ae-forgotten-export) The symbol "FileCarrier" needs to be exported by the entry point index.d.ts
//
// @public
export function imageFilesIn(carrier: FileCarrier | null | undefined): File[];

// @public
export interface ImageToInsert {
    alt?: string | null;
    extent: ImageExtent;
    src: string;
}

// @public (undocumented)
export const increaseIndent: Command;

// @public
export const increaseListLevel: Command;

// @public
export const insertFootnote: Command;

// @public
export function insertImage(image: ImageToInsert): Command;

// @public
export function insertImageFiles(view: EditorView, files: readonly File[]): Promise<void>;

// @public
export const insertLineBreak: Command;

// @public
export const insertPageBreak: Command;

// @public
export const insertTab: Command;

// @public
export function insertTable(input: TableSize): Command;

// @public (undocumented)
export function isBoldActive(state: EditorState): boolean;

// @public
export function isInList(state: EditorState): boolean;

// @public (undocumented)
export function isItalicActive(state: EditorState): boolean;

// @public (undocumented)
export function isStrikeActive(state: EditorState): boolean;

// @public (undocumented)
export function isUnderlineActive(state: EditorState): boolean;

// @public
export type LineSpacing = {
    rule: "auto";
    lines: number;
} | {
    rule: "exact";
    pt: number;
} | {
    rule: "atLeast";
    pt: number;
};

// @public (undocumented)
export type ListKind = "numbered" | "bullet";

// @public
export const lockSelection: Command;

// @public (undocumented)
export interface NewComment {
    // (undocumented)
    author: string;
    authorId?: string;
    date?: string;
    // (undocumented)
    initials?: string;
    // (undocumented)
    text: string;
}

// @public
const NOTE_KINDS: readonly ["footnote", "endnote"];

// Warning: (ae-forgotten-export) The symbol "NOTE_KINDS" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
type NoteKind = (typeof NOTE_KINDS)[number];

// Warning: (ae-forgotten-export) The symbol "ALIGNS" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export type ParagraphAlign = (typeof ALIGNS)[number];

// @public
export interface ParagraphStyleOption {
    hidden: boolean;
    // (undocumented)
    id: string;
    isDefault: boolean;
    name: string;
    primary: boolean;
}

// @public
export function readImageFile(file: File, maxWidthPx?: number): Promise<ImageToInsert | null>;

// @public (undocumented)
export const redo: Command;

// @public
export function removeComment(id: string): Command;

// @public
export function removeCommentReply(commentId: string, replyId: string): Command;

// @public
export const removeLink: Command;

// @public
export type RunToggle = "bold" | "italic" | "underline" | "strike";

// @public
export function selectComment(id: string): Command;

// @public
export type SelectionLock = "none" | "lockable" | "locked" | "mixed";

// @public
export function selectionLock(state: EditorState): SelectionLock;

// @public
export function selectionTouchesLocked(state: EditorState): boolean;

// @public
export function setCommentBody(id: string, body: Node_2): Command;

// @public
export function setCommentResolved(id: string, resolved: boolean): Command;

// @public
export function setFontFamily(name: string | null): Command;

// @public
export function setFontSize(pt: number | null): Command;

// @public
export function setFootnoteBody(id: string, body: Node_2): Command;

// @public
export function setLineSpacing(spacing: LineSpacing): Command;

// @public
export function setLink(url: string): Command;

// @public
export function setParagraphAlign(align: ParagraphAlign): Command;

// @public
export function setParagraphStyle(styleId: string | null): Command;

// @public
export function setTextBackground(hex: string | null): Command;

// @public
export function setTextColor(hex: string | null): Command;

// @public
export const SINGLE_LINE_SPACING: LineSpacing;

// @public
export interface TableSize {
    // (undocumented)
    columns: number;
    // (undocumented)
    rows: number;
}

// @public (undocumented)
export const toggleBold: Command;

// @public (undocumented)
export const toggleBulletList: Command;

// @public (undocumented)
export const toggleItalic: Command;

// @public (undocumented)
export const toggleNumberedList: Command;

// @public (undocumented)
export const toggleStrike: Command;

// @public
export const toggleUnderline: Command;

// @public (undocumented)
export const undo: Command;

// @public
export const unlockSelection: Command;

// @public
export function updateComment(id: string, text: string): Command;

// @public
export function updateCommentReply(commentId: string, replyId: string, text: string): Command;

// (No @packageDocumentation comment for this package)

```
